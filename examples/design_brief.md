# Example Design Brief

- Start from the original SVG drawing.
- Preserve parking, cores, columns, ramps, stairs, and the community outer shell.
- Merge cafe and small library into a greenery lounge when the standard allows it.
- Make the main hall the organizing connector.
- Use standards and ontology evidence before generating alternatives.
- Natural-language and doodle edits must become intent JSON before any native SVG patch is applied.
